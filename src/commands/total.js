const { SlashCommandBuilder } = require("discord.js");

module.exports = {
  // data: new SlashCommandBuilder()
  //   .setName("total")
  //   .setDescription("Prints out the total count"),
  // async execute(interaction, db) {
  //   try {
  //     interaction.reply(`Total bonks: ${await db.info.get("total")}.`);
  //   } catch (error) {
  //     console.error(error);
  //   }
  // },
};
